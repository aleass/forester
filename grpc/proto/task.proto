syntax = "proto3";

package proto;

option go_package = ".;proto";

message TaskReq {
  string Url = 1;
  int64 uuid = 2;
}

message Finish {
    int64 uuid = 1;
}

service Task {
    rpc SendTask (stream TaskReq)returns(stream Finish);
}